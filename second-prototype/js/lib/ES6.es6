// Demo module. Can be deleted at some point. May need to delete corresponding .js file as well.
class Car {
  constructor(name, year) {
    this.name = name;
    this.year = year;
  }
}

let prius = new Car('Prius', 2020);
